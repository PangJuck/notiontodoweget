# To-do 위젯 인수인계

Claude 채팅에서 설계와 노션 구축까지 끝냈다. 남은 로컬 작업을 Claude Code에서 이어받는다.

## 목표

- 아무 세션에서든 할 일을 말로 던지면 스킬이 정리해 노션에 기록
- 바탕화면에 항상 떠 있는 위젯이 노션을 읽어 일, 주, 월로 표시
- 완료된 항목이 쌓여 업무 기록이 된다

## 환경

- Windows, 파이썬 설치됨
- **노션 데스크톱 앱은 사용 불가.** 웹은 정상
- 노션 요금제는 무료 가정 (DB 자동화 편집 불가, 버튼 속성은 가능)

## 이미 끝난 것

### 노션

| 용도 | database_id | data_source_id |
|---|---|---|
| 업무 | `0e928040351d4fdfae49f77e67e914e6` | `5ead04e6-65a7-4e98-9b3c-a8dfffdf5001` |
| 개인 | `1730d225784340f88e15f9af9d51ea78` | `bd927ce1-075b-4b3a-b98a-7c823cbb6cfd` |

- 업무 DB는 회사 팀 페이지 안에 있다. **링크가 있는 사람 누구나 볼 수 있다**
- 개인 DB는 개인 워크스페이스에 있다
- 속성 (양쪽 동일): `할 일`(제목), `완료`(체크박스), `완료일`(날짜), `마감일`(날짜), `분류`(선택, 옵션 비어 있음), `메모`(텍스트)
- 뷰: 업무 쪽 4개 (할 일, 캘린더, 분류별, 기록), 개인 쪽 3개 (할 일, 캘린더, 기록)

### 파일

- `todo_widget.py` — 위젯 본체. 검증 완료
- `todo/SKILL.md` — 할 일 기록 스킬

## 사람이 직접 해야 하는 것

자동화 불가. Claude Code도 못 한다.

1. **노션 Integration 토큰 발급** — https://www.notion.so/profile/integrations, Internal, Capabilities에서 Read content와 Update content
2. **두 DB 각각에 연결 추가** — DB 페이지 우상단 ⋯ → 연결 → 해당 Integration. 한쪽만 하면 404
3. **완료 버튼 생성** — DB별로 속성 편집 → 새 속성 → 버튼 → 자동화 편집 → 이 페이지 편집 → `완료` 체크, `완료일` 오늘. 그리고 각 뷰에서 `완료` 체크박스 숨기기

## Claude Code가 할 것

1. `todo_widget.py`를 작업 폴더에 배치
2. `pip install pywebview`
3. 토큰 주입 방식 개선 — 아래 참고
4. 실행 확인, 에러 대응
5. 선택: `.pyw`로 바꿔 `shell:startup`에 바로가기 등록

### 토큰 주입

지금은 소스 상단 `TOKEN` 상수에 직접 넣는 구조다. 코드에 비밀값이 박히므로 개선 여지가 있다.

- 같은 폴더의 `.env` 또는 환경변수에서 읽도록 바꾸는 게 낫다
- 다만 위젯이 단독 실행되므로 환경변수는 startup 등록 시 안 잡힐 수 있다. 파일에서 읽는 쪽이 안전하다
- **토큰 값 자체는 이 문서에도 대화에도 남기지 않는다**

## 설계 결정과 이유

되돌리기 전에 읽어야 할 것들.

- **위젯이 HTML 단독이 아니라 파이썬인 이유**: 노션 API는 브라우저 CORS를 허용하지 않는다. 프론트엔드만으로는 절대 안 된다. 로컬 프로세스가 필수다
- **pywebview를 쓴 이유**: Windows 내장 Edge WebView2를 사용하므로 런타임 추가 설치가 없고, `on_top=True`로 항상 위 고정이 되어 PowerToys 같은 보조 도구가 불필요하다
- **DB를 둘로 나눈 이유**: 업무 DB가 링크 공유된 회사 페이지에 있다. 결혼 준비, 학업 같은 개인 항목이 섞이면 그대로 노출된다
- **완료일을 수식이 아니라 버튼으로 채우는 이유**: 노션 수식 2.0은 타입이 엄격해 미완료 시 빈 날짜를 반환하기 어렵다. 마지막 수정 시각은 완료 후 재수정 시 밀린다. 버튼은 모든 요금제에서 되고 클릭 한 번으로 두 값을 정확히 채운다
- **위젯이 이번 달 이후를 안 보여주는 이유**: 바탕화면 상주 위젯이 스크롤되면 안 본다. 개수만 한 줄로 표시한다
- **구글 캘린더 연동을 안 한 이유**: 목적이 정해지지 않았다. 밀어넣기는 중복과 양방향 동기화 문제를 낳는다. 나중에 붙인다면 iCal 비공개 주소로 읽기만 하는 방향

## 함정

- **노션 API 버전은 `2022-06-28` 고정.** 2025-09-03 이후 버전은 `databases/{id}/query`가 `data_sources/{id}/query`로 바뀌었다. 버전을 올리면 코드도 같이 고쳐야 한다
- **체크박스 필터 문법이 비대칭이다.** 뷰 DSL에서 미완료는 `FILTER "완료" = "__NO__"`, 완료는 `FILTER "완료" = true`. `"__YES__"`를 쓰면 미완료 필터로 잘못 파싱된다
- **속성명에 공백이 있다.** `할 일`. 붙여 쓰면 안 된다
- MCP의 `update_data_source`는 이 계정에서 승인이 안 떨어졌다. 스키마 수정이 필요하면 노션 UI에서 하거나 DB를 새로 만드는 편이 빠르다
- 개인 워크스페이스에 옛 `To-do 위젯` DB가 남아 있다. 휴지통으로 보내면 된다

## 나중에 볼 것

- 위젯에서 항목 추가 (지금은 읽기와 완료 처리만)
- 주간 회고를 스킬에 추가. 완료 항목을 분류별로 묶어 노션 페이지로 저장
- `날짜 미정` 항목이 쌓이면 무덤이 된다. 주 1회 꺼내 정리하는 루틴 필요
- 구글 캘린더 iCal 읽기
