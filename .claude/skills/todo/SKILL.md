---
name: todo
description: 할 일을 말이나 목록으로 던지면 노션 To-do DB에 정리해 기록한다. 조회, 완료 처리, 지난 기록 되짚기까지 포함. "할 일 추가", "투두", "이것들 정리해서 넣어줘", "오늘 뭐 남았지", "지난달에 뭐 했지" 같은 요청에 쓴다. 바탕화면 위젯이 같은 DB를 읽는다.
---

# To-do 기록

## 대상

DB가 둘이다. 구조는 같고 용도만 다르다.

| 용도 | data_source_id | 위치 |
|---|---|---|
| 업무 | `5ead04e6-65a7-4e98-9b3c-a8dfffdf5001` | 회사 팀 페이지 |
| 개인 | `bd927ce1-075b-4b3a-b98a-7c823cbb6cfd` | 개인 워크스페이스 |

**어디에 넣을지 먼저 정한다.** 업무 DB가 있는 페이지는 링크가 있는 사람 누구나 볼 수 있다. 결혼 준비, 학업, 병원, 가족, 개인 재정 같은 항목은 반드시 개인 DB로 간다. 회사 업무는 업무 DB로 간다. 어느 쪽인지 애매하면 넣기 전에 묻는다.

한 번에 여러 항목을 받으면 용도별로 나눠 두 번 호출한다.

속성명은 정확히 이대로다.

| 속성 | 타입 | 비고 |
|---|---|---|
| `할 일` | 제목 | 가운데 공백 있음 |
| `완료` | 체크박스 | 기록할 때 `"__YES__"`, `"__NO__"` |
| `마감일` | 날짜 | 캘린더 뷰의 축 |
| `분류` | 선택 | 옵션은 성준이 노션에서 직접 늘린다 |
| `완료일` | 날짜 | 완료 버튼이 채운다. 스킬이 완료 처리할 때도 함께 채운다 |
| `메모` | 텍스트 | 선택 |

노션 MCP로 접근한다. 바탕화면 위젯이 쓰는 Integration 토큰과는 별개다.

## 기록 규칙

1. 한 항목 = 한 페이지. 한 문장에 두 가지 일이 섞여 있으면 쪼갠다
2. `할 일`은 행동으로 끝나게 짧게 쓴다. "3PL 견적서 검토" 형태. 원문이 길면 요약하고 잘린 맥락은 `메모`로 옮긴다
3. `완료`는 항상 `"__NO__"`로 넣는다
4. `마감일`은 사용자가 시점을 말했을 때만 넣는다. **추측해서 채우지 않는다.** 비면 위젯이 "날짜 미정"으로 모은다
5. 상대 표현은 오늘 날짜 기준 절대 날짜로 바꾼다. 내일, 다음 주 화요일, 이번 달 말, 금요일까지 등
6. "곧", "조만간", "언제 한번"은 날짜를 비운다
7. `메모`는 원문에 부연이 있을 때만
8. `분류`는 **DB에 이미 있는 옵션 중에서만** 고른다. 새 값이 필요해 보이면 임의로 만들지 말고 묻는다. 애매하면 비운다

## 쓰기

`notion-create-pages`로 한 번에 전부 만든다. 항목마다 호출하지 않는다.

```
parent: {"data_source_id": "5ead04e6-65a7-4e98-9b3c-a8dfffdf5001"}   # 개인 항목이면 bd927ce1-075b-4b3a-b98a-7c823cbb6cfd
properties: {
  "할 일": "3PL 견적서 검토",
  "완료": "__NO__",
  "분류": "업무",
  "date:마감일:start": "2026-09-12",
  "date:마감일:is_datetime": 0,
  "메모": "가격보다 오배송률 위주로"
}
```

날짜가 없으면 `date:마감일:*` 키를 아예 뺀다. 시각까지 지정된 경우에만 `is_datetime`을 1로 두고 `start`에 ISO datetime을 쓴다.

## 조회

두 DB 모두에서 `완료`가 `"__NO__"`인 것만 가져와 합친 뒤 마감일 기준으로 묶는다. 항목마다 업무인지 개인인지 표시한다.

- 지난 것: 마감일 < 오늘. 있으면 항상 맨 위
- 오늘: 마감일 = 오늘
- 이번 주: 내일부터 이번 주 일요일까지 (월요일 시작)
- 이번 달: 그다음 날부터 월말까지
- 날짜 미정: 마감일 없음

"회사 것만", "개인 것만" 하면 해당 DB만 본다.

## 완료 처리

`notion-update-page`로 `완료`를 `"__YES__"`로 바꾸고, `date:완료일:start`에 오늘 날짜를 함께 넣는다. 둘 중 하나만 바꾸면 기록 뷰가 비거나 날짜가 빈다. 페이지를 지우지 않는다.

노션에서 직접 처리할 때는 체크박스 대신 `끝` 버튼을 누른다. 버튼이 두 값을 같이 채운다.

어떤 항목인지 애매하면 후보를 보여주고 고르게 한다. 짐작으로 처리하지 않는다.

## 지난 기록 되짚기

"지난주에 뭐 했지", "9월 정리해줘" 같은 요청이면 `완료`가 `"__YES__"`인 항목을 `완료일` 범위로 걸러 온다.

분류별로 묶어 보여주면 회고에 쓰기 좋다. 숫자만 나열하지 말고 무슨 일이 몰렸는지 한 줄로 짚는다.

`완료일`이 빈 완료 항목이 보이면 체크박스를 직접 눌러 처리한 것이다. 언제 끝냈는지 물어서 채워준다.

## 확인이 필요한 경우

- 한 덩어리를 여러 항목으로 쪼갰을 때는 결과를 먼저 보여주고 그대로 넣을지 묻는다
- 같은 내용이 이미 미완료로 있으면 중복으로 넣지 말고 알린다
- 새 `분류` 값이 필요해 보일 때

## 응답 형식

- 기록한 항목을 한 줄씩. 제목, 마감일, 분류만
- 이모지는 쓰지 않는다
- 가운뎃점 대신 쉼표를 쓴다
